import 'package:flutter/material.dart';

class UserMainpage extends StatelessWidget {
  const UserMainpage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(child: Text("MainPage"));
  }
}
